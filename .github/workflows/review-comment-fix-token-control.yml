name: review-comment fix token control

on:
  pull_request_review_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  control:
    if: >-
      startsWith(github.event.comment.body, '@gemini-cli /fix')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout default review-comment ref
        uses: actions/checkout@v4
      - name: Record inline-comment boundary
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          echo "EVENT_NAME=${GITHUB_EVENT_NAME}"
          echo "EVENT_ACTION=${{ github.event.action }}"
          echo "COMMENTER_ASSOCIATION=${{ github.event.comment.author_association }}"
          echo "GITHUB_REF=${GITHUB_REF}"
          echo "GITHUB_SHA=${GITHUB_SHA}"
          echo "CHECKED_OUT_SHA=$(git rev-parse HEAD)"
          echo "CHECKED_OUT_BRANCH=$(git branch --show-current)"
          pr_url='${{ github.event.pull_request.url }}'
          gh api "$pr_url" --jq '"PR_BASE_SHA=" + .base.sha + " PR_HEAD_SHA=" + .head.sha + " PR_HEAD_REPO=" + .head.repo.full_name + " PR_HEAD_FORK=" + (.head.repo.fork|tostring)'
      - name: Prove inline-comment contents write
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          run_key="${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          branch="review-comment-fix-token-control-${run_key}"
          marker_path=".authz-review-comment-fix-token-marker-${run_key}.md"
          marker='run-gemini-cli-review-comment-fix-token-control'
          base_sha="$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/heads/main" --jq '.object.sha')"
          gh api --method POST "repos/${GITHUB_REPOSITORY}/git/refs" \
            -f "ref=refs/heads/${branch}" -f "sha=${base_sha}" >/dev/null
          encoded_marker="$(printf '%s\n' "$marker" | base64 | tr -d '\n')"
          gh api --method PUT "repos/${GITHUB_REPOSITORY}/contents/${marker_path}" \
            -f message='test: record review-comment-fix token control' \
            -f "content=${encoded_marker}" -f "branch=${branch}" >/dev/null
          gh api "repos/${GITHUB_REPOSITORY}/contents/${marker_path}?ref=${branch}" \
            --jq '"MARKER_SHA=" + .sha'
          echo 'CONTROL_TOKEN_WRITE=branch+marker'
