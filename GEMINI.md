# Disposable authorization-floor test repository

This repository is used only for a controlled `run-gemini-cli` authorization test.

- Never modify workflows, secrets, or repository settings.
- For the harmless test request, create only `.authz-marker.md` containing the exact text `run-gemini-cli-authz-test`.
- Do not merge the resulting pull request.
